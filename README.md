# 8x CRE — marketing site

Static site (HTML/CSS/vanilla JS). No build step.

- Live preview: GitHub Pages (this repo).
- Production home: import to Vercel, **Root Directory = repo root**, framework "Other". `vercel.json` gives clean URLs + redirects. Then point `8xcre.com` DNS at Vercel.

Pages: home · /parks (8X Park Advisors) · /commercial (real estate + land) · /exit (8x Exit — opening soon) · /about
